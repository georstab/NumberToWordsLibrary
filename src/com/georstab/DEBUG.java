package com.georstab;

import java.math.BigDecimal;
import java.math.BigInteger;
import java.util.List;

public class DEBUG {

    // THIS CLASS IS JUST FOR COPY PASTE PURPOSE

    // TAKES UP TO 7 INTEGRAL and 7 DECIMAL DIGITS !
   // BigDecimal Input = new BigDecimal(12345.456);

    // Here we create the IntegralToList OBJECT
    // Then we assign the IntegralList to the IntegralData list so we can process them
   // IntegralToList a = new IntegralToList(Input);
   // List<BigInteger> IntegralData = a.integralToListM();


      //  System.out.println("Size:" + IntegralData.size());
      //  System.out.println("------------");
       // IntegralData.forEach(System.out::println);
       // System.out.println("------------");

    // Here we create the DemicalToList OBJECT
    // Then we assign the DecimalList to the DecimalData list so we can process them
   // DemicalToList b = new DemicalToList(Input);
   // List<BigInteger> DecimalData = b.DecimalToList();

       // System.out.println("Size :" + DecimalData.size());
       // System.out.println("------------");
        //DecimalData.forEach(System.out::println);
       // System.out.println("------------");
}
